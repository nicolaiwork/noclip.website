/** Filled in by Phase 2 (Task 10). */
export class GroundSampler {
    public eyeY(_nx: number, _ny: number, _nz: number, _eyeHeight: number, _dt: number): number | undefined { return undefined; }
}
